/**
 * Production Environment Configuration
 * Used when building for production (ng build)
 */
export const environment = {
  production: true,
  apiUrl: 'https://cozyliferpg-production.up.railway.app/api'
};
